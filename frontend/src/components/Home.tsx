import React, { useEffect } from "react";
const Home = () => {
 
  return (
    <main className="flex h-screen flex-col pt-3 py-10 px-8 bg-gray-800 text-gray-100 min-w-screen items-center justify-center">
      <header className="text-xl font-bold">Home</header>
    </main>
  );
};

export default Home;
